# ============================================================
# LOCAL — for local development
# PostgreSQL must be running locally (docker or installed)
# AWS credentials via: aws configure OR .env file
# ============================================================
spring:
  config:
    activate:
      on-profile: local

  jpa:
    show-sql: true

logging:
  level:
    "[com.demo.member]": DEBUG
    "[org.hibernate.SQL]": DEBUG
    "[org.springframework.web]": INFO
