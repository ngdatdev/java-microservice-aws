server:
  port: 8084

spring:
  application:
    name: master-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/master_db
    username: ${DB_USER:admin}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

services:
  member:
    url: ${MEMBER_SERVICE_URL:http://localhost:8081}
  file:
    url: ${FILE_SERVICE_URL:http://localhost:8082}

logging:
  level:
    root: INFO
    com.demo.master: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG